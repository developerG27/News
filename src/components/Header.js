import React from 'react';
import Header from './components/Header';

const Header = (props) => {
  return (
    <div>
      <Header 
        titolo = "Notizie"
      />
    </div>
  )
}

export default Header;